services:
  - type: web
    name: gold-price-predictor
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run gold_price_predictor.py --server.port=$PORT --server.address=0.0.0.0
